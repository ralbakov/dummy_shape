[project]
name = "dummy-shape"
version = "0.1.0"
description = "Test task. Calculation of areas of figures."
authors = [
    {name = "Ruslan Albakov",email = "albakov.ruslan@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"
